env: dev
database:
  dialect: sqlite
ingestion:
  mode: mock
llm:
  mode: mock
  providers_ref: providers.yaml
scheduler:
  scheduled_cron: "0 */6 * * *"
  event_cooldown_minutes: 30
budget:
  monthly_llm_budget: 0.0
  currency: USD
  alert_pct: 80
