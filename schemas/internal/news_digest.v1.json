{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://market-state-engine/schemas/internal/news_digest.v1.json",
  "title": "NewsDigest",
  "description": "Internal DTO v1. Weighted, ranked news bundle handed to LLM Call #1. From docs/contracts/internal-dtos.md §3.",
  "type": "object",
  "additionalProperties": false,
  "required": ["run_id", "items", "weighting_versions"],
  "properties": {
    "run_id": { "type": "string" },
    "items": {
      "type": "array",
      "items": { "$ref": "#/$defs/WeightedNewsItem" }
    },
    "weighting_versions": { "type": "object" }
  },
  "$defs": {
    "WeightedNewsItem": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "news_id",
        "title",
        "source",
        "published_at",
        "source_quality",
        "relevance",
        "recency_decay",
        "effective_weight"
      ],
      "properties": {
        "news_id": { "type": "string" },
        "title": { "type": "string" },
        "source": { "type": "string" },
        "published_at": { "type": "string", "format": "date-time" },
        "source_quality": { "type": "number", "minimum": 0, "maximum": 1 },
        "relevance": { "type": "number", "minimum": 0, "maximum": 1 },
        "recency_decay": { "type": "number", "minimum": 0, "maximum": 1 },
        "effective_weight": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    }
  }
}
